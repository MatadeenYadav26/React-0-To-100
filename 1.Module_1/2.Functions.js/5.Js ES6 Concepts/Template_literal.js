const name ="Aman";
const age = 21;

// const sentence = "My name is "+name+" and age is "+age;// instead of this we can use backticks wala

const sentence = `My name is ${name} and age is ${age}.`
console.log(sentence);
